// optional chaining ?.

let obj = {
  name:"harsh",
  age:26,
  address:{
    city:"bhopal"
  },
}

// obj.address.city 
// obj.address.city throws an error
// obj.address?.city no throws error it tells undefined 